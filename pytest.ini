[pytest]
# Scope collection to tests/ so a bare `pytest` from the repo root cannot
# wander into validation_3d/ or extracted analysis trees (some of which carry
# stray *_test.py files with hardcoded cluster paths that abort collection).
testpaths = tests
